services:
  postgres:
    image: postgres:15.8-alpine
    container_name: tickets_db
    env_file: ".env"
    ports:
      - 5432:5432
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data: